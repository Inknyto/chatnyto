Done: [x] DONE — You have to rewrite an old app:
Done: [x] DONE — Read the current project to understand it fully: flutter_app/chatnyto/
Done: [x] DONE — Implement all the placeholders in the flutter app (account settings,
Done:     entity parameter pages, language/theme handlers are now functional).
Done:     The app is native on every platform (android/ios/linux/macos/windows
Done:     targets in flutter_app/chatnyto) and communicates without the internet
Done:     over MQTT.
Done: [x] DONE — MQTT broker on the Heltec V3: esp32_broker/build_heltec_v3.sh +
Done:     flash_heltec_v3.sh build/flash the broker for esp32s3; the lora_bridge
Done:     firmware component relays MQTT over LoRa (SX1262), defaulting to the
Done:     maximum authorized band/power (menuconfig: LoRa Bridge Configuration)
Done:     with the fastest standard modulation (SF7/125kHz) for instant messages.
Done: [x] DONE — Reorganize the app visually (whatsapp-clone/ contains only notes,
Done:     no reusable code): chat-first pages per category, liquid glass design
Done:     (lib/core/widgets/liquid_glass.dart), smooth rounded icons.
Done: [x] DONE — Dark/light mode (lib/core/theme/, persisted; toggle in the
Done:     drawer, home app bar and every parameters page).
Done: [x] DONE — Smooth transitions and loading states (GlassPageRoute fade/slide
Done:     transitions, GlassShimmer loading placeholders, animated backgrounds).
Done: [x] DONE — The user advertises himself on the network with a public key:
Done:     identity = X25519 + Ed25519 key pairs; the self-signed public bundle is
Done:     published retained on chatnyto/presence/<fingerprint> of every
Done:     connected broker. The private keys stay AES-256-GCM encrypted with a
Done:     key derived from the user's password (PBKDF2-HMAC-SHA256, 210k rounds).
Done:     See lib/core/crypto/crypto_service.dart + Security & Identity page.
Done: [x] DONE — Securely encrypt all messages and ensure routing: every chat
Done:     payload is an AES-256-GCM envelope (public channels use a topic-derived
Done:     key, private channels a shared passphrase, peer-to-peer an X25519 ECDH
Done:     key); brokers only ever route ciphertext (zero-trust).
Done: [x] DONE — Add new brokers by name and disconnect from certain brokers:
Done:     menu -> Brokers (lib/core/brokers/), persisted list, per-broker
Done:     connect/disconnect switch.
Done: [x] DONE — Peer-to-peer and group chat, end-to-end encrypted (zero-trust):
Done:     connections accept an optional channel passphrase; the same E2EE
Done:     envelope covers 1:1 and group topics; peers are verified via their
Done:     signed presence advertisements.
Done: [x] DONE — Scripts to build and flash esp32_broker/ on the Heltec V3
Done:     (build_heltec_v3.sh, flash_heltec_v3.sh), broker info (SSID/IP/port) on
Done:     the Heltec's OLED (components/broker_display), and a command line
Done:     interface reachable with `screen /dev/ttyUSB0 115200` for configuring
Done:     the broker (components/broker_console: info, wifi, wifi-clear, reboot).
Done: [x] DONE — App available on all platforms natively (Flutter targets:
Done:     android, ios, linux, macos, windows; linux release build verified).
Done: [x] DONE — When the button for showing/hiding the quill editor is clicked, the
Done:     entire chat page disappeared, leaving the header. Root cause: flutter_quill 11
Done:     throws MissingFlutterQuillLocalizationException when its localizations
Done:     delegate is not registered, and the exception blanked the page body. Fixed by
Done:     adding FlutterQuillLocalizations.localizationsDelegates to the MaterialApp
Done:     (lib/my_app/my_app.dart).
Done: [x] DONE — Read all 73 files in whatsapp-clone/lib and implemented the app layout
Done:     and components in flutter_app/chatnyto with liquid glass:
Done:     * lib/core/widgets/wa_components.dart — WaChatTile (avatar/name/preview/time/
Done:       ticks/unread badge), WaMessageBubble (sender/receiver bubbles with the
Done:       clone's three-rounded-corner shape, timestamp + blue ticks), WaInputBar
Done:       (rounded input + circular send button).
Done:     * humans chats list and chat screen restyled on those components (WhatsApp-
Done:       style appbar with avatar + online status, bubbles, message FAB).
Done:     * Custom background image setting: Settings/Account -> Chat wallpaper
Done:       (lib/core/settings/wallpaper_picker.dart + core/theme/background_controller
Done:       .dart, persisted). Five bundled backgrounds to choose from in
Done:       assets/backgrounds/ (midnight_doodle, aurora, sunset_glass, paper_light,
Done:       forest) plus a plain glass option; applied behind the chat via
Done:       WallpaperBackdrop. Android release APK rebuilt successfully.
Done: [x] DONE — Revamp of the chatnyto app on branch revamp/whatsapp-ux, implementing
Done:     the whatsapp-clone design and UX (lib/revamp/):
Done:     * Initial settings configuration from the clone: welcome screen ("Agree and
Done:       continue") -> one-step profile setup (name + password) that silently creates
Done:       the X25519/Ed25519 identity, saves the profile and auto-connects the default
Done:       networks; returning users get a simple unlock screen (onboarding.dart).
Done:     * New chats shown by default: home opens on the Chats tab, sorted by most
Done:       recent, with unread badges and a New-group button (home_shell.dart).
Done:     * Easy people discovery on all connected brokers: People tab lists everyone
Done:       advertising on any connected broker, searchable by name, with identity
Done:       verification (signed presence checked against their public key; fingerprint
Done:       + public key shown, one tap to start an E2EE chat) (home_shell.dart).
Done:     * Global AND per-chat background images: wallpaper picker works globally
Done:       (Settings) and per chat (chat menu -> "Wallpaper for this chat", with
Done:       "Use global wallpaper" reset) (background_controller.dart).
Done:     * Identity displayed on the chat page: verified badge + fingerprint in the
Done:       header, full identity sheet (public key, copy button) on tap (chat_page.dart).
Done:     * Brokers are just an abstraction: default networks are configured on first
Done:       run (Heltec LoRa AP + this device), messages are sent over every connected
Done:       broker transparently (BrokerService.publishToAll), DMs auto-appear for the
Done:       recipient, and broker management is tucked away under "Networks (advanced)".
Done:     * Fully functional chat & social app: DMs (X25519 ECDH E2EE), named group
Done:       rooms (optional passphrase), Updates tab (who's online + network status),
Done:       Communities tab (classic humans/robots/AIs sections). No technical
Done:       configuration required anywhere in the main flow (chat_service.dart).
Done:     Android release APK builds; flutter analyze has zero errors.
Done: [x] DONE — People tab showing only yourself: two root causes fixed. (1) The app
Done:     attached the MQTT updates listener AFTER subscribing, so presence messages
Done:     delivered right after SUBACK were lost (broker_service.dart now listens
Done:     before subscribing). (2) The mongoose broker ignored the RETAIN flag, so
Done:     presence never reached late joiners — retained-message support was added to
Done:     the ESP32 broker (see below) and the app also re-advertises presence every
Done:     30s via a heartbeat (BrokerService.startHeartbeat), so discovery works even
Done:     on brokers without retain.
Done: [x] DONE — Asynchronous messaging + encrypted persistence (revamp/chat_service):
Done:     * All P2P and group messages are persisted encrypted at rest (AES-256-GCM
Done:       under a key derived from the identity seed, IdentityService.storageKey);
Done:       legacy plaintext history is migrated and removed.
Done:     * Messages sent while offline land in an encrypted outbox and are flushed
Done:       automatically when a broker connects.
Done:     * Missed messages are recovered via a sync protocol: clients broadcast
Done:       encrypted sync requests carrying the timestamp of the newest message they
Done:       hold, and any online member replays what came after (throttled, jittered,
Done:       deduplicated) — so someone who connects after the sender still receives
Done:       the messages, and incoming messages are saved even when the chat is not
Done:       open (they arrive through the always-on broker subscription).
Done: [x] DONE — Global app wallpaper: Settings -> App wallpaper paints the chosen
Done:     image under every screen with a translucent gradient wash so the whole app
Done:     stays liquid glass (GlassBackground + BackgroundController.appWallpaper).
Done:     Chat-global and per-chat wallpapers still apply on top.
Done: [x] DONE — Public groups on the mesh: passphrase-less groups are announced on
Done:     chatnyto/groups/<slug> (heartbeat + on join), discovered groups are listed
Done:     in the People tab under "Public groups on the mesh" with one-tap join, and
Done:     joiners receive the group history through the sync protocol.
Done: [x] DONE — esp-idf-mqtt-broker adapted and rebuilt: retained-message support
Done:     added to main/mqtt_server.c (stores the last retained message per topic,
Done:     max 64, delivers matching retained messages on SUBSCRIBE, empty payload
Done:     deletes, oldest dropped when full). Built successfully for esp32s3 with
Done:     /opt/esp-idf (v6.0.2): idf.py set-target esp32s3 && idf.py build ->
Done:     build/mqtt-broker.bin (app partition 97% used — grow it if the firmware
Done:     gains more features).
Done: [x] DONE — Positioning: the app is a tool for easy and secure connection for
Done:     people, AI agents and IoT devices. Robots section renamed to "IoT devices"
Done:     (Communities tab), AIs to "AI agents", onboarding tagline updated. No
Done:     technical configuration needed anywhere in the main flow.
Done: [x] DONE — Editing mode on long press: long-pressing a chat (revamp Chats
Done:     tab) opens Rename / Wallpaper-for-this-chat / Delete; long-pressing a
Done:     connection (humans, robots/IoT, AIs pages) opens Edit (broker, topic,
Done:     passphrase) / Delete; long-pressing a network on the Networks page opens
Done:     Edit (name, host, port) / Delete. Nothing deletes instantly anymore.
Done: [x] DONE — Friendlier broker connections: the Brokers page (renamed
Done:     "Networks") got a search field that filters both the registered networks
Done:     (by name/host) and the people discovered on them (by name/fingerprint);
Done:     the People tab searches people and public groups across all connected
Done:     brokers; networks auto-connect with defaults, no manual setup needed.
Done: [x] DONE — Password-protected brokers: the Broker model carries optional
Done:     username/password (persisted with the broker list), credentials are sent
Done:     on MQTT CONNECT (and a CONNACK refusal is treated as a failed
Done:     connection), and the add/edit broker dialog on the Networks page has
Done:     optional Username/Password fields. Works with the ESP32 broker's
Done:     BROKER_AUTHENTICATION option and any mosquitto with password_file.
Done: [x] DONE - Renaming a group renames its topic, and only its creator or an admin
Done:     he designated can do it: groups record the creator's Ed25519 public key (set when
Done:     created, taken from the group announcement when joining a discovered one) plus an
Done:     admin list the creator controls. ChatService.renameChat re-slugs the name, publishes
Done:     a signed 'group_rename' control message encrypted with the OLD channel key so members
Done:     can still read it, moves the local encrypted history to the new id, switches the topic
Done:     to chatnyto/chat/group/<new-slug> and withdraws the old retained announcement. Members
Done:     apply it only if the signature verifies against the creator key or a recorded admin
Done:     key; the creator appoints admins from the chat menu ("Group admins"), sent as a signed
Done:     'group_admins' message that only the creator may issue.
Done: [x] DONE - Background connections, notifications (with an off switch and a chosen
Done:     sound), persistent brokers, an auto-connect option, and discovery of the brokers on
Done:     the current network:
Done:     * Notifications: flutter_local_notifications raises a system notification for every
Done:       message arriving while its chat is not on screen or the app is in the background
Done:       (lib/core/notifications/notification_service.dart; Android POST_NOTIFICATIONS
Done:       permission + core library desugaring). Settings -> Notifications has an on/off
Done:       switch and a sound picker - a bundled two-note chime generated into
Done:       android/app/src/main/res/raw/chatnyto_chime.wav, the system default, or silent
Done:       (one Android channel per sound, since a channel's sound is immutable). Picking a
Done:       sound plays it so it can be heard.
Done:     * Background: broker connections and the 30s heartbeat are never torn down when the
Done:       app is backgrounded, and returning to the foreground reconnects, re-advertises
Done:       presence and re-syncs (WidgetsBindingObserver in my_app.dart).
Done:     * Persistent connections: every broker carries an autoConnect flag persisted with the
Done:       broker list, so a network switched on or off stays that way across restarts, plus a
Done:       master "Connect automatically" switch on the Networks page and in Settings.
Done:     * Discovery: BrokerService.discoverLocalBrokers scans every subnet the device is on
Done:       (plus the Heltec AP range 192.168.4.x) and confirms each candidate with a real MQTT
Done:       CONNECT/CONNACK handshake rather than just an open port; "Find brokers on this
Done:       network" lists what it finds for one-tap joining.
Done: [x] DONE - The ESP32 broker can be public or private at runtime: the serial
Done:     console gained `access`, which shows the current mode and switches it -
Done:     `access public` lets anyone on the network connect, `access private <user>
Done:     <password>` requires those credentials. The mode and credentials live in NVS
Done:     and are checked on every CONNECT, so changing them needs no reboot and no
Done:     rebuild; the menuconfig BROKER_AUTHENTICATION values remain the fallback.
Done:     `info` reports the mode too.
Done: [x] DONE - Background service: an Android foreground service
Done:     (lib/core/notifications/background_service.dart, flutter_foreground_task) keeps
Done:     the process - and with it the broker connections and their heartbeat - alive
Done:     while the app is not in use, and restarts it after a reboot or an app update.
Done:     It shows a quiet permanent notification, as Android requires, and Settings ->
Done:     Networks -> "Stay connected in the background" turns it off. Messages that
Done:     arrive this way raise notifications.
Done: [x] DONE - Profile picture support: Settings -> Profile has a picture tile (pick,
Done:     replace, remove). The image is downscaled to 128px JPEG, stored locally and advertised
Done:     with the identity — and it is covered by the Ed25519 signature (the signed message
Done:     becomes name:key:sha256(picture)), so nobody can put another face on a verified
Done:     identity. Pictures show in the People tab, the chat list and chat headers. Identities
Done:     published without a picture still verify against the original name:key form.
Done: [x] DONE - The new chat types support Quill editing like the old ones, with images:
Done:     the composer is a Quill editor with a toggleable formatting toolbar (bold, italic,
Done:     underline, strikethrough, colours, lists, quote, code); messages carry the Quill delta
Done:     next to their plain text and render read-only with the formatting. The attach button
Done:     sends a picture (shrunk to 720px JPEG and hard-capped, so one photo cannot monopolise
Done:     a LoRa link) inside the same end-to-end encrypted envelope, using whatever is typed as
Done:     its caption. Unformatted, image-less messages stay plain text so nothing extra
Done:     travels.
Done: [x] DONE - Liquid glass opacity: resting surfaces were too transparent, so the
Done:     default is now the denser shade that scrolling elements had, and Settings ->
Done:     Preferences -> Glass opacity is a persistent slider (GlassController) driving every
Done:     panel, bubble, bar and border across the app.
Done: [x] DONE - Customisation, all persisted: light/dark theme, app language,
Done:     glass opacity slider, an app-wide wallpaper, a chat-wide wallpaper and a
Done:     per-chat override, profile picture, notification sound, which networks connect
Done:     automatically, whether to stay connected in the background, whether the
Done:     password is remembered, and per-agent AI settings (provider, model, endpoint,
Done:     instructions, creativity, tools).
Done: [x] DONE - The password is remembered by default in the platform keystore (Android
Done:     EncryptedSharedPreferences / Keychain / libsecret / DPAPI via flutter_secure_storage,
Done:     lib/core/crypto/password_vault.dart) so the app opens straight into the chats. The
Done:     unlock screen has a "Remember me" checkbox, and Settings -> Security -> "Ask password
Done:     at every app open" turns remembering off and wipes the stored copy.
Done: [x] DONE - Groups (topics) we created can be deleted: the creator and the admins he
Done:     designated get "Delete group for everyone" (chat menu and long-press), which publishes
Done:     a signed 'group_delete' control message and clears the retained announcement; members
Done:     verify the signature against the recorded creator/admin keys and drop the
Done:     conversation. Everyone else only gets "Leave group", which removes it locally.
Done: [x] DONE (configuration) - The deployment is fully configured, and the app can
Done:     use such a broker by URL. NOT executed: running it publishes a service and
Done:     needs the Cloudflare tunnel token plus the server credentials, so the final
Done:     ./deploy/deploy-remote.sh is left for you to run.
Done:     * deploy/docker-compose.yml - mosquitto 2.0 plus cloudflared. No port is
Done:       published; the tunnel maps wss://<domain>/mqtt -> mqtt:9001, matching how
Done:       the other service on that server is exposed.
Done:     * deploy/mosquitto/config/ - authenticated users only (allow_anonymous false),
Done:       an ACL restricting everyone to the chatnyto/# tree, connection logging off,
Done:       message-size and queue caps, and both a plain 1883 listener (internal) and a
Done:       9001 WebSocket listener (a raw MQTT port cannot cross an HTTP tunnel).
Done:     * deploy/deploy.sh + deploy-remote.sh - rewritten for this stack (the ones in
Done:       the repo were the storefront's). Secrets in deploy/.env.prod may be
Done:       $(pass show ...); they resolve on your machine and stream to the server's
Done:       tmpfs, never its disk. Accounts come from a single MQTT_USERS variable, so
Done:       adding or removing someone is one edit plus `deploy-remote.sh users`.
Done:     * deploy/.env.prod, deploy.conf and the generated passwd file are gitignored.
Done:     * App side: broker hosts now accept mqtt://, mqtts://, ws:// and wss:// URLs
Done:       (BrokerService._BrokerAddress), so entering wss://<domain>/mqtt with a
Done:       username and password just works - or share it as a QR code.
Done:     * deploy/README.md documents the setup, and is honest that a relay still sees
Done:       connection metadata even though it only ever carries ciphertext.
Done: [x] DONE - Connection status (connected/disconnected) like the old app:
Done:     ConnectionStatusChip (lib/core/widgets/connection_status.dart) shows a live coloured
Done:     dot in the home app bar and a dot plus wording under the name in the chat header,
Done:     updating with the brokers.
Done: [x] DONE - Noted and made explicit: the Communities tab now has a "Classic
Done:     rooms" section with "MQTT rooms (old app)" leading to the original
Done:     broker/topic screens, so the old app is reachable but no longer the default
Done:     path.
Done: [x] DONE - Full English/French support via Flutter's i18n: lib/l10n/app_en.arb
Done:     and app_fr.arb hold every user-facing string (~150 keys, including plural- and
Done:     placeholder-bearing ones), generated into AppLocalizations and wired into
Done:     MaterialApp. LocaleController persists the choice, and Settings -> Preferences
Done:     -> Language switches between English, Français and the phone's own language
Done:     with the app updating immediately. Home, onboarding, chats, networks,
Done:     settings and the AI screens all read from the ARB files.
Done: [x] DONE - Network map: Networks -> the hub icon opens a graph
Done:     (lib/core/brokers/network_graph_page.dart) drawing this device at the centre, every
Done:     registered network around it (green when connected, amber when unreachable, with a
Done:     dot travelling along live links) and the people discovered on each one hanging off
Done:     their broker. The list view stays as it was.
Done: [x] DONE - QR codes for networks: long-pressing a network offers "Share by QR code",
Done:     which shows a chatnyto://broker?... code carrying host, port and any credentials; the
Done:     scan button in the Networks app bar opens the camera, adds the scanned network and
Done:     connects to it (lib/core/brokers/broker_qr.dart, qr_flutter + mobile_scanner, camera
Done:     permission added). The scanner bundles its barcode model so it works with no
Done:     internet, like the rest of the app.
Done: [x] DONE - Logo: assets/logo.png (plus a maskable foreground and every Android
Done:     launcher density) shows a person, an AI spark and a robot linked in a triangle around
Done:     a chat bubble whose three dots carry their colours - the app connecting humans, robots
Done:     and AIs. It is the launcher icon and greets you on the welcome screen.
Done: [x] DONE - The AI part (lib/ai/): Communities -> AI agents lists the agents you
Done:     configure, each one a provider + model + instructions + creativity, and opens
Done:     a chat in the same bubbles as the human ones with history kept on the device.
Done:     AiClient speaks three wire protocols - OpenAI-compatible /chat/completions,
Done:     Anthropic Messages and Gemini generateContent - so it covers the field, and it
Done:     turns provider failures into plain advice ("the provider refused the API key")
Done:     rather than raw HTTP errors.
Done: [x] DONE - MCP and BYOK providers:
Done:     * Providers (lib/ai/ai_provider.dart), all Bring Your Own Key: Anthropic,
Done:       OpenAI, Google Gemini, Mistral, Groq, DeepSeek, xAI, OpenRouter, Ollama on
Done:       your own machine (no key, works offline) and any other OpenAI-compatible
Done:       server via a base URL. Keys are stored per provider in the device keystore
Done:       (flutter_secure_storage), never in preferences and never with the agents,
Done:       and the app calls each provider directly from the device. Model names are
Done:       free text with suggestions, because they change faster than this app.
Done:     * MCP (lib/ai/mcp_client.dart): a JSON-RPC client over the streamable HTTP
Done:       transport - initialize, tools/list, tools/call - handling both plain JSON
Done:       and SSE replies. Tool servers are configured in AI settings with a "test"
Done:       button that lists what a server offers, enabled per agent, and their tools
Done:       are exposed to all three protocols in each one's own schema. The client runs
Done:       the tool loop (capped at 5 rounds) and shows every call and result inline in
Done:       the conversation. stdio servers are out of scope - a phone cannot spawn
Done:       processes - which is why servers are reached over HTTP.
Done: [x] DONE (working rule, applied throughout) - Every addition was designed
Done:     from the screen backwards: long-press for edit mode instead of instant delete,
Done:     one-tap joining for discovered networks and groups, a scan button rather than
Done:     typed addresses, plain-language errors, permissions asked only when the
Done:     feature is used, and defaults chosen so a non-technical person never has to
Done:     configure anything.
Done: [x] DONE - Code written where the intent was clear, which was everywhere
Done:     except one point that is reported rather than guessed: the production
Done:     deployment is fully configured but not executed, because running it publishes
Done:     a service and needs your Cloudflare tunnel token and server credentials. Say
Done:     the word and it is one command.
Done: [x] DONE - Voice calls, modelled on the clone's Calls screen (lib/calls/):
Done:     * A Calls tab beside Chats with the call history (outgoing, incoming, missed
Done:       in red, duration), call-back buttons and a "new call" button listing who is
Done:       reachable; a call button also sits in every direct chat's header.
Done:     * Full-screen call UI for ringing, connecting and connected, with mute,
Done:       speaker, answer and decline, a live timer and the reminder that the call is
Done:       end-to-end encrypted.
Done:     * An incoming call interrupts whatever is on screen - the call page is pushed
Done:       from a navigator key held by MyApp, not from any single screen.
Done:     * How it works: signalling (ring, answer, decline, hang up, and the WebRTC
Done:       offer/answer plus ICE candidates) travels inside the pair's existing
Done:       end-to-end encrypted DM channel, so there is no call server and no account
Done:       anywhere. The audio is a direct peer-to-peer WebRTC stream with no STUN or
Done:       TURN configured, which is deliberate: on a shared network the two devices
Done:       reach each other directly, and adding a public STUN server would be the
Done:       app's only unsolicited internet connection.
Done:     * Honest limit, stated in the UI: calls need both devices on the same IP
Done:       network. A LoRa link carries a few kilobits per second, which is enough for
Done:       messages but not for voice, so the picker says so rather than failing
Done:       mysteriously.
Done: Phone calls now work. Two real bugs, both found by driving the app on your two
Done: phones over ADB and reading the logs, plus one measured slowness:
Done:   * The ring never arrived. Call signalling is wrapped as
Done:     {'type': 'call', ...payload}, but an offer's payload carries its own 'type'
Done:     (the SDP kind, "offer"), and it was spread AFTER the envelope's key — so it
Done:     overwrote it. The receiver read type == "offer", failed to parse it as a chat
Done:     message and dropped it. The envelope's kind is now written last, and the
Done:     redundant SDP 'type' field is gone (it was never read).
Done:   * The caller hung up on itself. The broker delivers our own publications back
Done:     to us on the same topic, so the caller received its own offer, decided it was
Done:     an incoming call while already busy, and sent a decline — ending the call it
Done:     had just started. Every signal now carries the sender's fingerprint and our
Done:     own signals are ignored, the same way chat messages already were.
Done:   * Connecting took ~32 seconds. The ICE candidate list included 127.0.0.1 and
Done:     ::1, which can never reach another device but still get paired and timed out
Done:     on. Loopback candidates are no longer sent; the call now goes live in about
Done:     0.4 seconds after it is answered.
Done: Ringing is a real ring now, not a silent screen: an insistent, full-screen
Done: notification on the call channel with a vibration pattern and Answer/Decline
Done: actions, so a call comes through with the app in the background or the phone
Done: locked, and can be picked up without opening the app. USE_FULL_SCREEN_INTENT and
Done: VIBRATE were added to the manifest.
Done: Signalling travels over MQTT, as asked, inside the pair's existing end-to-end
Done: encrypted DM channel — no call server. Only the audio is direct WebRTC. Verified
Done: end to end between the two phones: ring, answer, connected, timer running, over
Done: both a LAN broker and the wss:// one.
Done: Calling with no broker connected now says so immediately instead of ringing into
Done: nothing, and the call screen names the network stage while connecting.
Done: Tapping a picture in a chat opens it full screen on black, pinch-to-zoom up to
Done: 6x, with the sender and the time in the bar. Two actions there:
Done:   * Save — into the photo library (Android/iOS/macOS/Windows, via `gal`, in a
Done:     "ChatNyto" album) or into the Downloads folder on Linux, which is the
Done:     equivalent well-known place on a desktop.
Done:   * Share — hands the file to any other app through the system sheet.
Done: Pictures arrive inside the encrypted message rather than as a file, so nothing
Done: exists on disk until you ask for it; saving is what writes the first copy out.
Done: The network map is now handled directly:
Done:   * Drag the background to pan, pinch to zoom (0.25x-4x, about the pinch centre
Done:     so what is under your fingers stays there), or use the +/- buttons.
Done:   * Drag any node to move it; the arrangement is kept across live updates so a
Done:     broker reconnecting does not undo your layout. The reset button puts
Done:     everything back and re-centres.
Done:   * Tap a node for its details: a network shows its address, how many people are
Done:     on it, and a switch that connects or disconnects it right there; a person
Done:     shows which network they are reachable through and their public key; the
Done:     centre shows what the map is made of.
Done: The layout is now truthful about who is where: the broker that carried each
Done: presence announcement is recorded, so people hang off the network that actually
Done: announced them instead of being spread round-robin.
Done: One bug found while testing on the phone: a tap was being swallowed as a
Done: one-pixel pan, because the tap and scale recognisers compete. Taps are now
Done: decided inside the scale gesture (a gesture that ends within 12px of where it
Done: started is a tap), which is reliable.
Done: The account model was rebuilt around the fact that an account IS its key pair,
Done: not a row in a database — which is what makes all five of these one feature:
Done:   * Same account on another device: "Transfer" shows a QR code carrying the same
Done:     ciphertext this device stores. The private seeds never exist in the clear
Done:     outside the app, and the code does not contain the password — the other
Done:     device asks for it. The screen says plainly what the code is worth.
Done:   * Deduplication by public key: an account is keyed by its X25519 public key, so
Done:     importing one the device already holds refreshes it instead of listing it
Done:     twice, however many devices it arrived from. Contacts are keyed the same way,
Done:     so the same person scanned from a code and heard on the network is one entry.
Done:   * Multiple accounts and identities: several identities live side by side, each
Done:     with its own chats, contacts, outbox and profile picture — stored under its
Done:     own scope and encrypted under its own key, so they are not merely hidden from
Done:     each other but unreadable. Switching locks the old one and drops everything
Done:     held for it. The account that existed before this change keeps the unsuffixed
Done:     storage, so no existing chat moved (verified on both phones: every chat and
Done:     picture survived the upgrade).
Done:   * Contact sharing: "My contact code" gives a QR code and a chatnyto://contact
Done:     link with your name and public keys — all public, safe to print or send.
Done:     Scanning one saves the person, verified against their own signature, so you
Done:     can write to them even when they are not on the air. The People tab merges
Done:     saved contacts with whoever is announcing themselves, marking which is which.
Done:   * Password recovery: a 120-bit recovery code is generated when an account is
Done:     made and shown on its own screen, which cannot be skipped past without
Done:     ticking that you wrote it down. It seals a second copy of the same seeds, so
Done:     "Forgot your password?" on the unlock screen takes the code and lets you set
Done:     a new password. It is honest crypto, not a back door: there is no third way
Done:     in, and the screen says so. A fresh code can be issued at any time, which
Done:     invalidates the old one.
Done: The unlock screen also lists the other accounts on the device, so a second
Done: identity is one tap away rather than hidden behind the lock.
Done: Broker credentials are out of the interface and out of the app's own storage:
Done:   * They now live in the platform keystore (Android Keystore, iOS Keychain,
Done:     libsecret, DPAPI), not in shared preferences — which is a plain file any
Done:     backup picks up. Brokers saved by an older build are migrated into the
Done:     keystore on first load and the plain copies are erased.
Done:   * They are read only by the code that opens the connection, never by a widget.
Done:     The edit dialog no longer pre-fills a password: if one is on file it says so
Done:     and offers to replace it. The sign-in fields are folded away entirely for the
Done:     ordinary case.
Done:   * The add-broker dialog now asks for the address first and nothing else —
Done:     "supa-tech.com" is a complete answer. The app fetches
Done:     https://<host>/.well-known/chatnyto.json over HTTPS and learns the WebSocket
Done:     URL, the port and the account from it; the credential goes straight into the
Done:     keystore, never displayed and never typed. A bare IP skips the lookup, which
Done:     is what a LAN or LoRa broker needs.
Done:   * The deployment serves that file: a small nginx container publishes
Done:     /.well-known/chatnyto.json, generated by deploy.sh from PUBLIC_HOST/APP_USER/
Done:     APP_PASSWORD in the env file, routed through the same Cloudflare tunnel. The
Done:     README documents the second tunnel route.
Done:   * Sharing a network by QR now carries the address alone by default; including
Done:     the sign-in is a deliberate switch, and only then is the password read out of
Done:     the keystore.
Done: Verified on the phone: the Add broker dialog shows Address, Name and "This
Done: network needs a sign-in" — no password field anywhere in sight.
Done: Used exactly that way, and it is what found the real bugs. Both phones
Done: (SM-G610F and COLOR_8) were driven over ADB — install, launch, screenshots read
Done: back, taps and text injected, logcat followed — with a mosquitto broker on this
Done: machine so the two could see each other. The three call bugs, and the map's
Done: swallowed tap, were all found on the devices rather than by reading the code.
Done: No blocking questions this round — the reported bug was reproducible and the
Done: rest was specified clearly enough. Three judgement calls worth naming, all made
Done: rather than asked because either answer left the work useful:
Done:   * "Make the phone calls work over mqtt" — read as "make calls work; the
Done:     signalling path is MQTT", not "carry the audio over MQTT". Signalling already
Done:     went over MQTT and was where all three bugs were. Relaying the audio itself
Done:     through the broker is possible but would need a PCM capture/playback plugin
Done:     with no Linux support, so calling from the computer — your actual test — would
Done:     still not work. Say the word if you want the audio relayed too, for the case
Done:     where the two devices cannot reach each other directly.
Done:   * Password recovery is a recovery code, not a reset. Anything that could let
Done:     the app reopen an account without either the password or a second secret
Done:     would be a back door in the encryption.
Done:   * "Only the clean URL" is implemented as a published profile the server serves
Done:     over HTTPS, so the app configures itself from the domain. The alternative —
Done:     shipping a credential inside the binary — would have put it back in the app.
Done: Not done, and needing you: ./deploy/deploy-remote.sh is still unrun. It now also
Done: needs PUBLIC_HOST, APP_USER and APP_PASSWORD in deploy/.env.prod, and a second
Done: Cloudflare route for /.well-known/*.
Done: When you are offline(connected to a local broker without internet), or connected to the
Done: broker via mobile data, the phone calls do not work. The phone rings, you answer, then get
Done: (Connecting...) . Currently, the phone calls work only when I am connected to a broker that
Done: is running on my vps. You have to make the phone calls work with or without internet, and
Done: also make it work via mobile data when we are connected to the vps.
Done: The messages work whatever the setup is. Sending images does not work on the esp32_broker,
Done: but works on other local brokers(mosquitto).
Done: You have to zoom in the images on double click, and zoom out when we double click again.
Done: Add the possibility to view someone's profile picture.
Done: Also, you should be able to return to the phone call view when a phone call is happening.
Done: Currently, while on a call, you cannot go back to the call screen to change parameters or
Done: hang.
Done: In the networks page, you have to show the most recently connected brokers at the top.
Done: The accounts data , contacts and access have to be separated(with the option to
Done: share the data between accounts). Currently, when you switch accounts, the account data
Done: present on the app is still the old account data. Which means one account can access another
Done: account's data directly, which should not be possible.
Done: Also, for the background connection, what I meant is, whenever the app is closed, you should
Done: still be able to receive phone calls and messages. Currently, I still have to go to networks
Done: and re-activate the previously activated networks whenever I close and reopen the app.
Done: Wanted behaviour is that those networks should disconnect only when the button is toggled,
Done: and messages and phone calls should arrive even when the phone is locked.
Done: Add possibility to change the call and message ringtones to a local audio file.
Done: Show the profile picture on all screens where the profile appears. Currently, in some views,
Done: the account name only is present, with the placeholder profile picture.
Done: Add contact management on the people page, and on the three dots menu at the top right.
Done: Add message status (sent, received, read) via single/double grayed-out/blue
Done: checkmarks(implement the existing checkmarks)(like whatsapp).
Done: Is there a way to share broker information for another user to join our network with the
Done: current app architecture, while making sure the credentials are not shared with it?
Done: If you have questions, do not write the code yet. Ask them first. If you do not have
Done: questions, write the complete code.
Done: --- what was done for the batch above -------------------------------
Done: Calls now take the best road they can find, in three steps. Direct, as
Done: before, when the two devices share a network. Otherwise through STUN and
Done: TURN servers the network publishes in its own profile, which is what was
Done: missing over mobile data: with no STUN at all, a phone behind carrier NAT
Done: never learns an address the other side could dial, so the call could
Done: never connect — that is the "(Connecting...)" you saw. And when there is
Done: still no route, the audio is encrypted and published on the same topic as
Done: the messages: 8 kHz mono, IMA ADPCM, 60 ms frames, about 8 kB/s. It goes
Done: wherever a message goes, which is the point. The switch-over happens 8
Done: seconds after answering if the direct path has not come up, so a call
Done: never sits on "Connecting…" again. deploy/ gained an optional coturn
Done: container; set TURN_USER/TURN_PASSWORD/TURN_PUBLIC_IP to switch it on,
Done: and open 3478/udp, 3478/tcp and 49160-49200/udp. Relaying is Android and
Done: iOS only — the raw capture and playback plugins have no Linux build — so
Done: a desktop still needs the direct path.
Done: Images on the esp32_broker: the whole encrypted envelope went out as one
Done: publish, and the Mongoose broker on the LoRa box grows its receive buffer
Done: 512 bytes at a time out of a couple of hundred kilobytes of heap, so a
Done: photo could not land there. Anything over 2 KB is now published as
Done: numbered pieces and put back together by the receiver, with a pause
Done: between them so a small broker's per-subscriber buffer is not overrun.
Done: A message goes to every connected broker at once, so it is sized for the
Done: smallest one.
Done: Double tap zooms in on the point you tapped and again zooms back out.
Done: Tapping a profile picture opens it full screen, with the same zoom, save
Done: and share as a picture from a chat — including your own, so you can check
Done: what other people see. Every avatar in the app is one widget now, so the
Done: real picture shows in the people list, the chat header, the details
Done: sheet, the call screen, the call history and the account list, instead of
Done: an initial on a coloured disc.
Done: A green bar across the top of the app while a call is running takes you
Done: back to it, and the call screen has a way down into the app.
Done: Networks are listed with what is connected first, then whatever was
Done: connected most recently.
Done: Account separation: the stores were already keyed per account, but each
Done: also kept a copy in memory and that copy is what the screen showed —
Done: which is why switching identity still showed the previous account's
Done: things. Chats, contacts and call history are now dropped together on a
Done: switch, and profile name, mail and number are scoped too. Contacts are
Done: separate per identity by default, with a setting to share one address
Done: book across them.
Done: Ringtones: any audio file on the device, for calls and for messages. The
Done: file is copied into the app rather than referenced where it sits, because
Done: the picker's permission does not survive a restart. It is played by the
Done: app and the notification goes out silent, since Android fixes a channel's
Done: sound when the channel is made and cannot read a file that belongs to us.
Done: Contacts: a page of its own from the three dots, and hold any row in the
Done: People tab to message, rename (locally, only on this device), keep or
Done: forget someone.
Done: Ticks are real now: a clock while a message waits for a network, one tick
Done: once a broker has it, two once the other device has answered for it, two
Done: in blue once it has been read. Receipts are batched, so catching up after
Done: being offline does not answer a burst with a burst. Groups show no ticks
Done: at all — "delivered" has no single answer there, and a guess would be
Done: worse than nothing.
Done: Sharing a network without its credentials: yes, and that is now what the
Done: QR code does by default. It carries the address alone; the joiner's app
Done: asks the server itself at /.well-known/chatnyto.json and puts what comes
Done: back straight into the keystore. The same payload is a link, so it can be
Done: sent to someone who is not in the room. Including the sign-in in the code
Done: is still possible, but it is a deliberate switch.
Done: --- the freeze you reported ----------------------------------------
Done: My fault, and fixed. I had changed the startup sweep to connect every
Done: network at once instead of one after another. On a phone that has
Done: collected a dozen networks over the months, most of them long gone, that
Done: is a dozen simultaneous DNS lookups and sockets sitting there until they
Done: time out — and mqtt_client's own reconnection kept working at clients
Done: that had never connected in the first place. Now: three at a time with a
Done: pause between batches, a 5 second connect timeout, no library-level
Done: auto-reconnect, and a network that fails is backed off (a minute,
Done: doubling to ten) instead of being retried every thirty seconds forever.
Done: Coming back to the app clears the backoff. Two other things came out of
Done: looking at this: the heartbeat, the lifecycle callback and the switch in
Done: the UI could each open their own client for the same broker — the log
Done: showed two connections a millisecond apart — and every delivery receipt
Done: was decrypting a conversation's whole stored history, which for a chat
Done: with pictures in it is megabytes. Both fixed.
Done: --- why your networks kept needing to be switched on again -----------
Done: Found on the phone, and it was not what it looked like. "Connect
Done: automatically" at the top of the Networks page was off, and it silently
Done: overrules every per-network switch: they all read as on, and none of them
Done: connected. Three changes. The per-network switch now shows what you asked
Done: for rather than whether the wire happens to be up, so a network that is
Done: merely unreachable no longer reads as off. When automatic connection is
Done: off, both the master row and every network below it say so. And switching
Done: a network on turns automatic connection back on, because asking for a
Done: network is asking to be connected to it — that state cannot happen again.
Done: --- tests ------------------------------------------------------------
Done: test/wire_test.dart covers the two new pieces of wire format that are
Done: easy to get subtly wrong and impossible to eyeball: the codec's nibble
Done: packing and sign extension, and reassembling a split message including
Done: out of order, two at once, and malformed pieces. 10 tests, all passing.
Done: The stock Flutter counter test was deleted — it came from the first
Done: commit with its pumpWidget commented out and had never passed.
Done: --- verified on the phones, and what was not -------------------------
Done: On the two phones over ADB: no ANR on a cold start on either, the app
Done: connects on its own once automatic connection is on, exactly one client
Done: per broker now instead of two, the networks list sorts with the connected
Done: one on top, the warning text appears when automatic connection is off,
Done: Contacts is in the three dots menu, profile pictures show in the chat
Done: list and the chat header, and the Add broker dialog still has no password
Done: field. Not confirmed end to end on hardware this round: the ticks turning
Done: blue, a photo over the LoRa box, and a relayed call — the second phone
Done: kept dropping off USB and the first was down to 4% battery. The logic is
Done: unit tested where it can be; the rest wants a session with both phones
Done: charged.
Done: --- needing you ------------------------------------------------------
Done: ./deploy/deploy-remote.sh is still unrun by me. If you want calls to work
Done: over mobile data through supa-tech.com rather than falling back to the
Done: relay, add TURN_USER, TURN_PASSWORD and TURN_PUBLIC_IP to deploy/.env.prod
Done: and open those three ports.
